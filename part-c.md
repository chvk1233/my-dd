Part C. `training_events_1200.csv` 기반 텍스트 전처리 과제

목표

완성형 관제센터에서 사건은 결국 `message`로 들어옵니다.
AI 분석을 하려면 이 문장을 모델이 이해할 수 있는 숫자 형태로 바꿔야 합니다.

오늘은 다음 흐름을 실습합니다.

message
-> tokenization
-> integer encoding
-> one-hot encoding

한국어로 쓰면 다음과 같습니다.

매장 사건 문장
-> 토큰화
-> 정수 인코딩
-> 원-핫 인코딩

원-핫 인코딩은 학습 구조를 이해하기 좋은 방식입니다.
실제 딥러닝 서비스에서는 이후 워드 임베딩으로 넘어가는 경우가 많지만, 오늘은 원-핫 인코딩까지 직접 확인합니다.



C-1. CSV 데이터 구조 확인

작업
`training_events_1200.csv`를 불러오고, `message` 컬럼과 label 역할을 하는 컬럼을 확인하세요.

학생용 프롬프트
> training_events_1200.csv를 사용해 텍스트 전처리 실습을 하려고 한다.
> 
> 실습 목표:
> - CSV를 불러온다.
> - message 컬럼을 확인한다.
> - label 또는 event_type 역할을 하는 컬럼을 찾는다.
> - 결측치가 있는지 확인한다.
> 
> Colab 또는 Python 코드로 다음을 셀 단위로 작성해줘.
> 
> 1. pandas import
> 2. CSV 읽기
> 3. df.head() 출력
> 4. df.info() 출력
> 5. 컬럼 목록 출력
> 6. message 예시 5개 출력
> 7. label 역할을 하는 컬럼 후보 출력
> 
> 주의사항:
> - 코드만 주지 말고 각 셀에서 무엇을 확인해야 하는지 설명해줘.
> - 컬럼명이 예상과 다르면 어떻게 찾아야 하는지도 알려줘.

프롬프트를 쓰는 이유

전처리는 데이터 구조 확인에서 시작합니다.
컬럼을 확인하지 않고 토큰화부터 하면 엉뚱한 데이터를 처리할 수 있습니다.

학생이 답해야 할 질문

1. CSV에는 어떤 컬럼이 있는가?
2. `message` 컬럼은 무엇을 의미하는가?
3. label 역할을 하는 컬럼은 무엇인가?
4. 결측치가 있다면 어떻게 처리해야 하는가?



C-2. 토큰화 실습

작업
`message` 문장을 토큰화하세요.
먼저 단순 띄어쓰기 기준으로 토큰화하고, 가능하면 한국어 형태소 분석 결과와 비교하세요.

학생용 프롬프트
> training_events_1200.csv의 message 컬럼을 토큰화하려고 한다.
> 
> 다음 실습 코드를 만들어줘.
> 
> 1. message 샘플 10개를 가져온다.
> 2. split()으로 띄어쓰기 기반 토큰화를 한다.
> 3. 가능하면 Okt 같은 한국어 형태소 분석기로 토큰화한다.
> 4. 두 결과를 표로 비교한다.
> 5. 어떤 단어가 사건 판단에 중요해 보이는지 표시한다.
> 
> 중요한 기준:
> - 숫자, 시간, 온도, 금액 같은 정보는 무조건 지우지 않는다.
> - 예를 들어 "10분 지연", "2번 결제", "온도 12도"는 의미가 있을 수 있다.
> - 토큰화 결과에서 사건 판단에 중요한 단어가 사라지면 안 된다.
> 
> 코드 뒤에는 다음을 설명해줘.
> - split() 토큰화의 장점
> - split() 토큰화의 한계
> - 한국어 형태소 분석이 필요한 이유

프롬프트를 쓰는 이유

토큰화는 문장을 작은 조각으로 나누는 작업입니다.
하지만 아무렇게나 자르면 중요한 정보가 사라질 수 있습니다.

학생이 답해야 할 질문

1. 띄어쓰기 토큰화 결과는 어떤 장점이 있는가?
2. 띄어쓰기 토큰화만으로 부족한 이유는 무엇인가?
3. 사건 판단에 중요한 토큰 5개를 고르면 무엇인가?
4. 숫자나 단위를 무조건 삭제하면 왜 위험한가?



C-3. 정수 인코딩 실습

작업
토큰화 결과를 바탕으로 단어 사전을 만들고, 문장을 정수 시퀀스로 바꾸세요.

학생용 프롬프트
> 
> 토큰화된 message 데이터를 정수 인코딩하려고 한다.
> 
> 다음 실습 코드를 만들어줘.
> 
> 1. 토큰화된 문장 리스트를 만든다.
> 2. 전체 토큰 빈도수를 계산한다.
> 3. 가장 많이 등장한 토큰 20개를 출력한다.
> 4. word_to_index 딕셔너리를 만든다.
> 5. index_to_word 딕셔너리를 만든다.
> 6. 각 문장을 정수 시퀀스로 변환한다.
> 7. 처음 보는 단어를 위한 <UNK> 토큰을 둔다.
> 8. 직접 만든 방식과 Keras Tokenizer 방식도 비교한다.
> 
> 설명 기준:
> - 단어 사전이 왜 필요한지 설명해줘.
> - 정수 인코딩은 단어의 의미를 이해하는 것이 아니라 번호를 붙이는 것임을 설명해줘.
> - 문장마다 길이가 달라지는 이유를 설명해줘.

프롬프트를 쓰는 이유

모델은 문자를 그대로 계산하지 못합니다.
정수 인코딩은 토큰을 숫자 ID로 바꾸는 단계입니다.

학생이 답해야 할 질문

1. 단어 사전은 왜 필요한가?
2. `<UNK>` 토큰은 왜 필요한가?
3. 정수 인코딩만으로 단어 의미를 알 수 있는가?
4. Keras Tokenizer를 쓰면 편하지만 내부 구조를 이해해야 하는 이유는 무엇인가?



C-4. 원-핫 인코딩 실습

작업
정수 인코딩 결과를 원-핫 인코딩으로 바꾸고 벡터 구조를 확인하세요.

학생용 프롬프트
> 
> 정수 인코딩된 문장을 원-핫 인코딩으로 바꾸는 실습을 하려고 한다.
> 
> 먼저 전체 CSV가 아니라 작은 샘플 문장 3개로 구조를 확인하고 싶다.
> 
> 다음 코드를 만들어줘.
> 
> 1. 샘플 문장 3개 선택
> 2. 토큰화
> 3. word_to_index 만들기
> 4. 정수 인코딩
> 5. numpy로 직접 원-핫 벡터 만들기
> 6. Keras to_categorical로 원-핫 인코딩 만들기
> 7. 원-핫 벡터 shape 출력
> 8. vocabulary size가 커지면 어떤 문제가 생기는지 설명
> 
> 주의사항:
> - 원-핫 인코딩은 구조 이해용으로 작은 샘플에 먼저 적용한다.
> - 전체 데이터에 크게 적용하면 메모리 부담이 생길 수 있음을 설명한다.
> - 원-핫 인코딩과 워드 임베딩의 차이를 짧게 비교한다.

프롬프트를 쓰는 이유

원-핫 인코딩은 단어를 구분하기 쉽지만, 단어 수가 많아지면 벡터가 매우 길어집니다.
그래서 작은 샘플로 구조를 먼저 보는 것이 중요합니다.

학생이 답해야 할 질문

1. 원-핫 인코딩에서 1은 무엇을 의미하는가?
2. vocabulary size가 커지면 벡터는 어떻게 되는가?
3. 원-핫 인코딩의 장점은 무엇인가?
4. 원-핫 인코딩의 한계는 무엇인가?
5. 왜 실제 딥러닝에서는 임베딩을 더 자주 사용하는가?



C-5. 전처리와 프로젝트 연결 설명

작업
전처리 실습이 완성형 관제센터에서 어디에 들어가는지 설명하세요.

학생용 프롬프트
> 
> 오늘 실습한 토큰화, 정수 인코딩, 원-핫 인코딩이 AI 매장 운영 관제센터에서 어디에 들어가는지 설명해줘.
> 
> 다음 흐름을 기준으로 설명해줘.
> 
> Dashboard에서 사건 message 입력
> -> Flask /ingest
> -> Kafka message 또는 저장소에 기록
> -> Consumer 또는 AI 분석 함수
> -> 토큰화
> -> 정수 인코딩
> -> 원-핫 인코딩 또는 임베딩
> -> 모델 추론
> -> event_type / severity
> -> Dashboard 표시
> 
> 설명 기준:
> 1. 오늘 실습한 전처리가 어느 위치에 들어가는지
> 2. 전처리를 Flask에서 할 수도 있고 AI 분석 함수에서 할 수도 있는 이유
> 3. Kafka가 있으면 전처리 위치를 어떻게 생각해야 하는지
> 4. 원-핫 인코딩은 학습 이해용이고 실제 서비스에서는 임베딩으로 넘어갈 수 있는 이유
> 5. 현재 기본 구조에서 완성형 AI 분석 구조로 확장되는 점


주의사항:
- "AI가 알아서 한다"라고 설명하지 마.
- message가 어떤 단계를 거쳐 숫자가 되고, 다시 분석 결과가 되는지 단계별로 설명해줘.

프롬프트를 쓰는 이유

전처리 실습은 Colab에서 끝나는 것이 아닙니다.
관제센터에서는 사용자가 입력한 `message`가 분석 가능한 숫자 형태로 바뀌어야 합니다.

학생이 답해야 할 질문

1. 전처리는 UI에서 하는가, 서버에서 하는가, AI 분석 함수에서 하는가?
2. Kafka가 있으면 전처리 위치는 어떻게 달라질 수 있는가?
3. `message`가 `event_type`, `severity`로 바뀌는 흐름을 본인 말로 설명하세요.
4. 현재 기본 구조와 오늘 완성형 구조의 차이는 무엇인가?
