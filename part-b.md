Part B. 기본 Dashboard UI를 완성형 관제센터 UI로 확장하기

목표

기본 Dashboard UI는 사건 목록, 선택 사건 상세, Zustand store 중심 구조까지 갖고 있습니다.
오늘은 이 구조를 완성형 관제센터처럼 보이도록 확장합니다.

완성형 UI의 기준은 다음입니다.

API 상태 패널
+ 사건 목록
+ 선택 사건 상세
+ 분석 요약 패널
+ 보고서/체크리스트 mock 패널
+ 오류/fallback 상태 표시



B-1. 현재 Zustand store와 컴포넌트 분석하기

작업

Cursor AI에게 현재 프론트엔드 구조를 분석하게 하세요.

학생용 프롬프트
> 
> 현재 프로젝트의 프론트엔드 구조를 분석해줘.
> 
> 나는 기본 Dashboard와 기본 백엔드 흐름을 갖고 있는 상태라고 가정한다.
> 현재 UI에는 사건 목록, 선택 사건 상세, Zustand store 기반 상태 관리가 있다.
> 
> 먼저 코드를 수정하지 말고 아래 내용을 찾아서 설명해줘.
> 
> 1. Zustand store 파일은 어디에 있는지
> 2. store가 가진 state 목록
> 3. store가 가진 action 목록
> 4. 사건 목록 컴포넌트는 어디에 있는지
> 5. 사건 상세 컴포넌트는 어디에 있는지
> 6. API 호출 함수는 어디에 있는지
> 7. fallbackEvents는 어디에서 쓰이는지
> 8. 완성형 관제센터 UI로 확장하려면 어떤 state/action/component가 추가로 필요한지
> 
> 출력 형식:
> - 현재 구조 표
> - 추가해야 할 구조 표
> - 현재 기본 구조에서 확장되는 점 요약
> 
> 주의사항:
> - 코드는 아직 수정하지 마.
> - 정답 경로를 찾으려고 하지 말고 현재 프로젝트 안에서 판단해줘.

프롬프트를 쓰는 이유

UI를 확장할 때 가장 먼저 봐야 하는 것은 디자인이 아니라 상태 흐름입니다.
Zustand store에 어떤 값이 있고 어떤 action이 있는지 알아야 새 패널을 붙여도 흐름이 무너지지 않습니다.

학생이 답해야 할 질문

1. `events`는 왜 store에 들어가는가?
2. `selectedEventId`와 `selectedEvent`는 무엇이 다른가?
3. `selectedEvent`를 store에 직접 저장하지 않는 이유는 무엇인가?
4. API 실패 시 `fallbackEvents`가 있으면 어떤 장점이 있는가?



B-2. 완성형 UI 확장 기준 설계하기

작업

기본 Dashboard UI를 다음 화면 구조로 확장하는 설계를 작성하세요.

추가할 UI 영역

UI 영역 : 역할
API 상태 패널 : Flask API 연결 상태 표시
사건 목록 : 기존 기능 유지
선택 사건 상세 : 기존 기능 유지
분석 요약 패널 : selectedEvent의 분석 결과 또는 fallback 분석 표시
보고서 mock 패널 : 점장 보고서 예시 표시
체크리스트 mock 패널 : 직원 조치 항목 예시 표시
오류/fallback 표시 : API 실패 시 사용자에게 현재 상태 안내


학생용 프롬프트
> 
> 기본 Dashboard UI를 완성형 관제센터 UI로 확장하려고 한다.
> 
> 현재 출발점:
> - 사건 목록
> - 선택 사건 상세
> - Zustand store
> - fallbackEvents
> - loadEvents()
> - selectEvent(eventId)
> 
> 추가하고 싶은 화면:
> - API 상태 패널
> - 분석 요약 패널
> - 보고서 mock 패널
> - 체크리스트 mock 패널
> - 오류/fallback 상태 표시
> 
> 다음을 설계해줘.
> 
> 1. 어떤 컴포넌트를 새로 만들지
> 2. 어떤 기존 컴포넌트를 수정할지
> 3. Zustand store에 어떤 state를 추가할지
> 4. Zustand store에 어떤 action을 추가할지
> 5. API가 없어도 mock/fallback으로 화면을 유지하는 방법
> 6. 화면 배치를 초급자도 이해할 수 있게 설명
> 7. 현재 기본 구조에서 무엇이 확장되는지
> 
> 주의사항:
> - 아직 코드를 바로 쓰지 말고 구조 설계부터 해줘.
> - UI는 예쁘게 꾸미는 것보다 데이터 흐름이 보이게 만드는 것이 우선이다.
> - AI 모델이나 외부 LLM API는 오늘 연결하지 않는다.

프롬프트를 쓰는 이유

완성형 UI는 단순히 카드 몇 개를 추가하는 것이 아닙니다.
어떤 상태가 store에 들어가야 하고, 어떤 값은 선택된 사건에서 계산할 수 있는지를 판단해야 합니다.

학생이 답해야 할 질문

1. API 상태 패널은 어떤 정보를 보여줘야 하는가?
2. 분석 요약 패널은 어떤 데이터를 기준으로 보여줄 수 있는가?
3. 보고서와 체크리스트를 mock으로 둬도 오늘 퀴즈 목표에 맞는 이유는 무엇인가?
4. 기본 Dashboard UI에서 완성형 UI로 넘어가며 가장 크게 바뀐 구조는 무엇인가?



B-3. UI 확장 구현 프롬프트

작업

Cursor AI로 UI를 확장하세요.
단, AI가 만든 코드를 그대로 믿지 말고 상태 흐름을 검토해야 합니다.

학생용 프롬프트
> 
> 현재 프론트엔드 UI를 완성형 관제센터 UI처럼 확장해줘.
> 
> 구현 목표:
> 1. 기존 사건 목록과 선택 사건 상세 기능은 유지한다.
> 2. API 상태 패널을 추가한다.
> 3. 선택된 사건을 기준으로 분석 요약 패널을 추가한다.
> 4. 보고서 mock 패널을 추가한다.
> 5. 체크리스트 mock 패널을 추가한다.
> 6. API 실패 또는 데이터 없음 상태를 화면에 표시한다.
> 7. Zustand store 구조를 크게 망가뜨리지 않는다.
> 
> 중요한 제약:
> - 정답 코드를 복사하는 방식이 아니라 현재 코드에서 최소 확장한다.
> - AI 모델, 외부 LLM API, Hugging Face 모델은 오늘 연결하지 않는다.
> - mock 데이터와 fallback으로 완성 흐름을 보여줘도 된다.
> - 수정 전 어떤 파일을 바꿀지 먼저 말해줘.
> - 수정 후 어떤 화면을 확인해야 하는지 체크리스트를 만들어줘.
> 
> 마지막에 다음을 설명해줘.
> - 추가된 state/action
> - 추가된 component
> - 현재 기본 구조에서 확장된 점
> - 내가 직접 검증해야 할 부분

프롬프트를 쓰는 이유

이 프롬프트는 UI 확장의 기준을 “예쁜 화면”이 아니라 “관제센터 데이터 흐름”에 둡니다.
정답 코드를 복제하는 것이 아니라, 기본 Dashboard UI를 발전시키는 것이 핵심입니다.

학생이 답해야 할 질문

1. 새로 추가된 UI 영역은 무엇인가?
2. 어떤 state/action이 추가되었는가?
3. API가 꺼져도 화면이 유지되는가?
4. 사용자가 이 UI를 보면 사건의 상태를 파악할 수 있는가?
