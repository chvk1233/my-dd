기본 Dashboard에서 완성형 관제센터로 확장하기

퀴즈 목적

오늘 퀴즈는 정답 코드를 보고 따라 만드는 문제가 아닙니다.
현재 여러분이 가진 **기본 Dashboard와 백엔드 흐름**을 출발점으로 삼아, Cursor AI를 활용해 완성형 프로젝트로 확장하는 과제입니다.

AI를 사용해도 됩니다.
다만 AI에게 "그냥 다 만들어줘"라고 시키는 것이 아니라, **왜 이 프롬프트를 쓰는지**, **어떤 구조를 만들려는지**, **AI 결과 중 무엇을 받아들이고 무엇을 수정했는지**를 반드시 설명해야 합니다.

* 필수 항목
>> 코드를 절대로 수정하지 않는다.
>> 수정이 필요하다면 사용자의 동의를 구한다.

현재 출발점

여러분은 기본 Dashboard와 기본 백엔드 흐름을 갖고 있다고 가정합니다.

Frontend 출발점

현재 UI는 다음 기능까지 갖고 있다고 봅니다.

• 사건 목록 조회
• 선택 사건 상세 표시
• Zustand store 기반 상태 관리
• `events`, `selectedEventId`, `loading`, `error`
• `loadEvents()`, `selectEvent(eventId)`
• API 실패 시 `fallbackEvents`로 화면 유지

즉, 아직 완성형 관제센터는 아니지만, **사건 목록과 선택 상태를 관리하는 기본 Dashboard**는 있는 상태입니다.

Backend 출발점

현재 백엔드는 다음 흐름까지 배웠다고 봅니다.

• Kafka message 구조
• Consumer가 message를 받는 흐름
• `model_inference.py` 같은 분석 함수 위치
• 분석 결과 저장 또는 조회 흐름
• Flask `/events` 조회 API의 필요성
• 실제 AI 모델이 없어도 `mock` 또는 `fallback`으로 흐름을 완주하는 방식

즉, 아직 완성형 API 서버는 아니지만, **사건을 받고 분석 결과를 저장/조회해야 한다는 구조**는 알고 있는 상태입니다.

오늘의 완성 기준

오늘 완성해야 할 것은 거대한 AI 서비스가 아닙니다.
기준은 다음입니다.

기본 Dashboard UI
-> 완성형 관제센터 UI로 확장
-> Flask API 서버와 연결 가능한 구조 만들기
-> AI/LLM은 mock 또는 fallback으로 처리
-> training_events_1200.csv로 텍스트 전처리 흐름 이해



제출물

아래 내용을 제출하세요.

1. 내가 Cursor AI에 입력한 프롬프트 목록
2. 각 프롬프트를 쓴 이유
3. AI가 제안한 구조 요약
4. 내가 받아들인 부분과 거절하거나 수정한 부분
5. 현재 기본 구조에서 무엇이 확장되었는지
6. 실행 또는 검증 결과
7. 텍스트 전처리 실습 결과

AI 답변을 그대로 붙여 넣으면 안 됩니다.
반드시 본인 말로 재구성하세요.
